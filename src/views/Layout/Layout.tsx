
const Layout: React.FunctionComponent = ({ children }) => {
    return (
        <div>
            <h1>Layout</h1>
            <main>
                {children}
            </main>
        </div>
    )
};

export default Layout;

